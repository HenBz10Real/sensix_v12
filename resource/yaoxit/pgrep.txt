status=$(pgrep -f plugin.dex) 
	if [ "$status" ]; then
    rm /sdcard/ramdump/log.txt
		echo "program berhasil berjalan"> /sdcard/ramdump/log.txt
		rm /sdcard/ramdump/plugin.dex
		
	else
    rm /sdcard/ramdump/log.txt
		echo "program failed"> /sdcard/ramdump/log.txt
		rm /sdcard/ramdump/plugin.dex
	fi
